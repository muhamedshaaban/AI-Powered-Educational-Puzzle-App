import 'package:flutter/material.dart';

class TeacherConfirmScreen extends StatelessWidget {
  const TeacherConfirmScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}